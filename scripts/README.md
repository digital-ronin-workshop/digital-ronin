# Automation Scripts

> Скрипты для автоматизации: проверки системы, wikilinks, синхронизация

---

## 🤖 Автоматические проверки (встроены в workflow)

### parse-todos.sh

**Когда:** Автоматически при `начать сессию` (каждый день)

**Что делает:**
- Парсит TODO markers в .md файлах
- Находит skeleton agents (Status: Skeleton)
- Показывает прерванные задачи из session-tracker.md

**Ручной запуск:**
```bash
./scripts/parse-todos.sh
```

---

### audit-quick.sh (НОВОЕ!)

**Когда:** Автоматически при `закрыть сессию` или `итоги` (каждый день)

**Что делает (5-10 секунд):**
- Проверяет синхронизацию версий
- Ищет новые skeleton agents
- Показывает прерванные задачи

**Особенность:** НЕ блокирует закрытие сессии — просто напоминание

**Ручной запуск:**
```bash
./scripts/audit-quick.sh
```

**Exit code:** Всегда `0` (не блокирует)

---

### audit-freshness.sh

**Когда:** Автоматически при `шарим` (только пятница)

**Что делает (15-30 секунд):**
- Всё из audit-quick.sh +
- Глубокая проверка personal data в конструкторе
- Поиск старых цен во ВСЕХ файлах
- Проверка готовности к раздаче

**Ручной запуск:**
```bash
./scripts/audit-freshness.sh
```

**Exit codes:**
- `0` — всё ОК, готов к синхронизации
- `1` — найдены проблемы, БЛОКИРУЕТ синхронизацию

---

## 📊 Двухуровневая система

| Проверка | Когда | Скорость | Блокирует? |
|----------|-------|----------|------------|
| **parse-todos** | Начать сессию | 3-5 сек | Нет |
| **audit-quick** | Закрыть сессию | 5-10 сек | Нет (напоминание) |
| **audit-freshness** | Шарим (пятница) | 15-30 сек | ДА (критично) |

---

## 🔗 Wikilinks Management

---

## add-wikilinks.sh

Автоматически добавляет Related секции в файлы агентов, которые их не имеют.

**Использование:**
```bash
./scripts/add-wikilinks.sh
```

**Что делает:**
- Находит все `.md` файлы в `agents/` (кроме `_*` и `README.md`)
- Проверяет наличие секции `## Related`
- Добавляет базовую Related секцию если её нет

---

## validate-wikilinks.sh

Проверяет все wikilinks в проекте на наличие broken links.

**Использование:**
```bash
./scripts/validate-wikilinks.sh
```

**Что делает:**
- Находит все wikilinks `[[...]]` в `.md` файлах
- Проверяет существование файлов
- Выводит список broken wikilinks
- Показывает общую статистику

---

## Примечания

- Скрипты уже имеют права на выполнение (`chmod +x`)
- Работают на macOS и Linux
- Безопасны: не модифицируют файлы без проверки
